window.Vendas=(()=>{
  const listar=()=>DB.carregar().vendas;
  const registrar=d=>{let criada;DB.alterar(db=>{const cliente=db.clientes.find(c=>c.id===d.clienteId),itens=d.itens.map(i=>{const quantidade=Number(i.quantidade),precoOriginal=Number(i.precoOriginal??i.precoUnitario),precoFinalUnitario=Number(i.precoFinalUnitario??precoOriginal),custoUnitario=Number(i.custoUnitario||0),subtotalOriginal=quantidade*precoOriginal,subtotalFinal=quantidade*precoFinalUnitario,custoTotal=quantidade*custoUnitario;return{produtoId:i.produtoId,nome:i.nome,quantidade,precoOriginal,precoFinalUnitario,custoUnitario,subtotalOriginal,subtotalFinal,custoTotal,lucro:subtotalFinal-custoTotal,precoUnitario:precoFinalUnitario}}),subtotalOriginal=itens.reduce((s,i)=>s+i.subtotalOriginal,0),valorFinal=itens.reduce((s,i)=>s+i.subtotalFinal,0),descontoTotal=subtotalOriginal-valorFinal,custoTotal=itens.reduce((s,i)=>s+i.custoTotal,0),lucro=valorFinal-custoTotal,saldoAnterior=cliente?Number(cliente.saldo||0):0,saldoAtual=d.status==='fiado'?saldoAnterior-valorFinal:saldoAnterior;criada={id:Utils.uuid(),clienteId:d.clienteId||null,clienteNome:cliente?.nome||'Venda avulsa',itens,subtotalOriginal,descontoTotal,valorFinal,valorTotal:valorFinal,custoTotal,lucro,status:d.status,data:new Date().toISOString(),observacao:d.observacao||'',saldoAnterior,saldoAtual,ajusteManual:Boolean(d.ajusteManual),descontoTipo:d.descontoTipo||null};db.vendas.push(criada);itens.forEach(i=>{const p=db.produtos.find(x=>x.id===i.produtoId);if(p&&p.estoque!==null)p.estoque=Math.max(0,p.estoque-i.quantidade)});if(cliente){cliente.totalComprado=Number(cliente.totalComprado||0)+valorFinal;cliente.quantidadeVendas=Number(cliente.quantidadeVendas||0)+1;cliente.ultimaCompra=criada.data;if(d.status==='fiado')cliente.saldo=saldoAtual}db.movimentacoes.push({id:Utils.uuid(),clienteId:d.clienteId||null,clienteNome:criada.clienteNome,tipo:'venda',vendaId:criada.id,valor:valorFinal,status:d.status,data:criada.data});if(descontoTotal!==0)db.movimentacoes.push({id:Utils.uuid(),clienteId:d.clienteId||null,clienteNome:criada.clienteNome,tipo:'desconto',vendaId:criada.id,valor:descontoTotal,data:criada.data});if(d.ajusteManual)db.movimentacoes.push({id:Utils.uuid(),clienteId:d.clienteId||null,clienteNome:criada.clienteNome,tipo:'ajuste_valor_venda',vendaId:criada.id,subtotalOriginal,valorFinal,data:criada.data})});return criada};
  const ultima=()=>{const vendas=listar();return vendas[vendas.length-1]||null};
  const podeDesfazer=()=>{const v=ultima();return Boolean(v&&Date.now()-new Date(v.data).getTime()<=5*60*1000)};
  const desfazerUltima=()=>{let removida;DB.alterar(db=>{const venda=db.vendas[db.vendas.length-1];if(!venda)throw Error('Nenhuma venda para desfazer');if(Date.now()-new Date(venda.data).getTime()>5*60*1000)throw Error('O prazo de 5 minutos para desfazer terminou');removida={...venda};db.vendas.pop();venda.itens.forEach(i=>{const p=db.produtos.find(x=>x.id===i.produtoId);if(p&&p.estoque!==null)p.estoque+=Number(i.quantidade)});const cliente=db.clientes.find(c=>c.id===venda.clienteId);if(cliente){if(venda.status==='fiado')cliente.saldo=Number(venda.saldoAnterior||0);cliente.totalComprado=Math.max(0,Number(cliente.totalComprado||0)-Number(venda.valorFinal??venda.valorTotal));cliente.quantidadeVendas=Math.max(0,Number(cliente.quantidadeVendas||0)-1);const anteriores=db.vendas.filter(v=>v.clienteId===cliente.id);cliente.ultimaCompra=anteriores.length?anteriores[anteriores.length-1].data:null}db.movimentacoes=db.movimentacoes.filter(m=>m.vendaId!==venda.id);db.movimentacoes.push({id:Utils.uuid(),clienteId:venda.clienteId,clienteNome:venda.clienteNome,tipo:'venda_desfeita',vendaId:venda.id,valor:Number(venda.valorFinal??venda.valorTotal),data:new Date().toISOString()})});return removida};
  return{listar,registrar,ultima,podeDesfazer,desfazerUltima};
})();
