window.Fiados=(()=>{const listar=()=>DB.carregar().clientes.filter(c=>c.ativo&&Number(c.saldo)<0),receber=(clienteId,valor,observacao)=>{let pagamento;const operationId=Utils.uuid();DB.alterar(db=>{const c=db.clientes.find(x=>x.id===clienteId),divida=Math.abs(Math.min(0,Number(c.saldo)));const v=Math.min(Number(valor),divida);if(!v||v<=0)throw Error('Informe um valor válido');c.saldo=Number((c.saldo+v).toFixed(2));pagamento={id:operationId,operationId,clienteId,clienteNome:c.nome,tipo:'pagamento',valor:v,saldoAnterior:Number((c.saldo-v).toFixed(2)),saldoNovo:c.saldo,data:new Date().toISOString(),observacao:observacao||''};db.pagamentos.push(pagamento);db.movimentacoes.push({...pagamento})});return pagamento};return{listar,receber}})();
